お疲れ様です、GitBucketです！ <%= pusher %> さんがPushしましたよ。

<%= owner %> / <%= repo %>
<%= url %>
<% commits.forEach(function(commit) { %>
----------------------------------------------------------------
Title: <%= commit.title %>
<% if (commit.desc) { %><%= commit.desc %><% } %>
Commit: <%= commit.id %>
Url   : <%= commit.url %>
<%= commit.timestamp %>
<% commit.modified.forEach(function(modified_file) { %>  MOD : <%= modified_file %><% }); %>
<% commit.added.forEach(function(added_file) { %>  ADD : <%= added_file %><% }); %>
<% commit.removed.forEach(function(removed_file) { %>  DEL : <%= removed_file %><% }); %>
<% }); %>

[コミットランキング]
<% ranking.forEach(function(rank, i) { %><%= i+1 + " " + rank.name + " : " + rank.point + "point\n" %><% }); %>
