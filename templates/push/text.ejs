お疲れ様です、GitBucketです！ <%= pusher %> さんがPushしましたよ。

<%= owner %> / <%= repo %>
<%= url %>
<% commits.forEach(function(commit) { %>
----------------------------------------------------------------
Title: <%= commit.title %>
<% if (commit.desc) { %><%= commit.desc %><% } %>
Commit: <%= commit.id %>
Url   : <%= commit.url %>
<% commit.changed_files.forEach(function(file) { %> <%= file.type + " : " + file.name + "\n"%><% }); %>
<%= commit.timestamp %>
<% }); %>

[コミットランキング]
<% ranking.forEach(function(rank, i) { %><%= i+1 + " " + rank.name + " : " + rank.point + "point\n" %><% }); %>
